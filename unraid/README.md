# Future Unraid packaging

Stage 1 has no production container or Unraid template. This directory is
reserved for documented packaging after the Node.js production server and
container have been implemented and tested.

The intended host and container port is `6532/tcp`.
