# Game engine package

This package is reserved for the framework-independent game engine planned for
a later stage. It will contain tested pure functions for generic board sizes,
path validation, open-dictionary checks, scoring, and duplicate detection.

Stage 2 intentionally contains no game-engine implementation.
