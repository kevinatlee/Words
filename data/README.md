# Data location

Production game data is kept inside the private server-oriented workspace at
`packages/game-data`, where runtime-relative paths, package tests, provenance,
and generated defaults stay together.

The root `data/` directory remains unused. See `docs/GAME_DATA.md` for the
pinned dictionary source, exact licence notice, reproducible generation,
checksums, audits, and Stage 4B boundary.
