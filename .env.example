# Stage 2 local defaults. Copy to .env only if your own shell or process
# manager loads it; the application does not commit or require a real .env.
PORT=6532
PUBLIC_BASE_URL=<public origin>
MAX_PLAYERS=8
MAX_ROOMS=500
ROOM_TTL_MINUTES=120
RECONNECT_GRACE_SECONDS=60
CLEANUP_INTERVAL_SECONDS=30
