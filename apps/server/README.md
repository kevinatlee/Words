# Server application

This directory is reserved for the future Node.js, Express, and Socket.IO
server. The server will own temporary room state, timers, boards, validation,
scores, and host authority.

Stage 1 intentionally contains no functional server.
